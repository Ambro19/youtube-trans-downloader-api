# ===============================
# BACKEND (.env.example)
# Copy to .env and fill in values
# ===============================
# backend/.env.example
# Copy to backend/.env and fill locally. Do NOT commit backend/.env.

# --- Database ---
DATABASE_URL=sqlite:///./youtube_trans_downloader.db

# --- JWT AUTH ---
SECRET_KEY=replace_with_random_64_chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# --- Stripe (Test or Live) ---
# IMPORTANT: Never put real keys in example files to avoid secret scanning.
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
STRIPE_SECRET_KEY=__STRIPE_SECRET_KEY_PLACEHOLDER__        # DO NOT use a real sk_ key here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here   # Public, but keep a placeholder to be safe

# Lookups (recommended) — these are safe to commit
STRIPE_PRO_LOOKUP_KEY=pro_monthly
STRIPE_PREMIUM_LOOKUP_KEY=premium_monthly

# Optional product names fallback (safe)
STRIPE_PRO_PRODUCT_NAME=Pro Plan
STRIPE_PREMIUM_PRODUCT_NAME=Premium Plan

# --- App environment ---
ENVIRONMENT=development
HOST=0.0.0.0
DOMAIN=http://localhost:8000
#FRONTEND_URL=http://localhost:3000
